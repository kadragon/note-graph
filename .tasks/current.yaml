# Current Task
# Only one task should be active at a time

current_task: TASK-039
notes: |
  Starting TASK-039 (SPEC-project-1): Implement R2 file upload and storage.
  Creating ProjectFileService and POST /projects/:projectId/files endpoint.
  Supporting multipart/form-data with file validation (type, size limits).
