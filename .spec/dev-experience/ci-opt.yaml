spec_id: SPEC-devx-2
title: "Optimize CI Workflow"
given_when_then:
  - given: "The current CI workflow runs lint, typecheck, build, and test in separate jobs"
    when: "A pull request is opened or updated"
    then: "These steps run sequentially in a single job to reduce overhead and duplicate setup time"
acceptance_tests:
  - id: TEST-devx-ci-1
    desc: "The CI workflow contains a single job named 'ci' or similar"
  - id: TEST-devx-ci-2
    desc: "The job executes npm ci, lint, typecheck, build, and test in order"
dependencies:
  - governance: "env"
linked_tasks:
  - TASK-CI-OPT
